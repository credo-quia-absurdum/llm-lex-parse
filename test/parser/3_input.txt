struct _point {
    int x;
    int y;
    int z;
    int color;
    char* name;
} point;


int main() {
  if (!(a > 5)) return (a+1);

  while (*m.a[3] < 5 || x.b == 5 && *c != *(x.d) || &(e[5])+1 >= d%e--) {
    a = 1;
    b = *a/2;
    c = a+1;
    d = -(b*3/2%a+1-x);
    e = (&b-3)+(c--);
  }

  for (i = 0; i <= (a*b+1-c); i++) {
    if (b == 5) break;
    else continue;
  }
  return 0;
}